#indexing and slicing
matrix = np.array([1,2], [2,3])
#full rows and full clms
#first colm
#second colm
#0,1 and 1,3 submatrix


#booling indexing

#elemnet wise opertion
#sum,mult....


#mathematical operations like sin ,sqrt,

# dot product np.dot
# basic statistics mean ,median,...


# data = np.random.randomint()
#np.percentile
#sum along the row or certain colm
#2D statistics mean,matrix
#array manupulation



matrix = np.array([1,2],[3,4])

matrix = arr.reshape(3, 4)

print("reshape",)
print("flatent",{matrix.flatten[]})
print("ravel",{matrix.ravel[]})


# reasigning the value at index o
# print(matrix)
# f = 
# r = 
#convolutional CNN to used to data 

# concatination
a = np.array([1,2])
b = np.array([3,4])

print("Horizontal",{[np.concatinate[]].shape})
print("Horizontal(hstack)",{[np.hstack[]].shape})
#split an array into sub arrs